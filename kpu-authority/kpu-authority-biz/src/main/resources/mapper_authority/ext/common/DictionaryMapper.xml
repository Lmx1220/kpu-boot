<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lmx.kpu.authority.dao.common.DictMapper">

    <select id="pageItem" resultType="cn.lmx.kpu.authority.entity.common.Dict" parameterType="map">
        select type, label, max(state) state, min(created_time) created_time from c_dictionary
        <where>
            <if test="query.label != null and query.label != ''">
                and (type like #{query.label,typeHandler=fullLike} or label like #{query.label,typeHandler=fullLike} or
                name like #{query.label,typeHandler=fullLike})
            </if>
            <if test="query.state != null ">
                and state = #{query.state}
            </if>
        </where>
        group by type,label
        order by created_time DESC
    </select>

</mapper>
